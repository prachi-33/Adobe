[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run start"
  port = 8888

# Optional: add redirects or headers here
# [[redirects]]
#   from = "/api/*"
#   to = "/.netlify/functions/:splat"
#   status = 200
